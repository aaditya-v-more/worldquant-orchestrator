.venv/
__pycache__/
*.pyc
data/
.pytest_cache/

# never commit credentials or sessions, even if someone moves them in-repo
*credentials*.json
*session*.json
.DS_Store

# generated alpha candidate batches — inputs to `sim batch`, not source
ideas/

# per-user account facts (level, score, slots, which endpoints 403). Several
# people share this repo on different accounts; committed docs must not assert
# any of it. Regenerate with `wqo account snapshot`.
ACCOUNT.local.md
